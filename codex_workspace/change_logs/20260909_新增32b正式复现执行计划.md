# 20260909 新增《正式复现执行计划_32b定版》文档

## 变更内容
- 新增文件：`codex_workspace/reports/20260909_正式复现执行计划_32b定版.md`
- 目的：将论文实验矩阵、run_server_batch.sh 工具链矩阵、模型选型结论（qwen2.5:32b）固化为可对照执行的分阶段复现计划。

## 背景
- fd 修复 commit `5a373a1` 已推送；32b/27b 在当前代码下均验证稳定（2 集完整落盘）。
- 用户选定 qwen2.5:32b（27b 单次调用慢 4~5 倍、资源占用高）。

## 文档要点
1. 验收标准：复现成功率趋势与作者参考一致（intersection: 0shot 74% / 2shot 90% / 5shot 80% / no-neg 15%；merge: 75% / 85% / 84% / 33%）。
2. 执行顺序：自检 → 建库冒烟（验证 siliconflow 嵌入）→ Part1 建库+0shot → Part2 → Part3。
3. 关键提醒：
   - `.env.server` 已是 32b，切模型改 `.env.server`（batch 脚本会覆盖 .env）；
   - `llm_controller/result/`、`excel/`、`db/` 被 .gitignore，结果需 scp/rsync 同步，不能 git；
   - 2shot/5shot 依赖 Part1 记忆库，Part1 必须先跑完。
