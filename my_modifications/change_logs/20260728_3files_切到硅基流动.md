# 20260728 - 将 LLM 切换到硅基流动（SiliconFlow）

## 📅 修改时间
2026-07-28

## 📂 修改的原项目文件

| 文件 | 用途 |
|------|------|
| `llm_controller/llm_agent_action.py` | 单车 LLM 决策模块 |
| `llm_controller/llm_agent_negotiation_system.py` | 多车协商模块 |
| `llm_controller/memory.py` | 记忆向量库的嵌入类 |

## 🎯 修改目的

原代码调用 **OpenAI 的 `gpt-4o-mini`**，存在两个问题：
1. **国内服务器访问 OpenAI 不稳定**（需 VPN 代理 `http://127.0.0.1:7890`）
2. **API Key 硬编码** `your key here`，推到 git 不安全

切换到 **硅基流动 (SiliconFlow)**，原因：
- 国内直连，速度快、稳定
- 提供 OpenAI 兼容接口，几乎零代码改动
- 有免费额度（Qwen2.5-7B-Instruct），适合复现阶段
- API Key 用环境变量管理，不进 git

## 🔧 具体改动

### 1. `llm_controller/llm_agent_action.py`

**第 1-15 行（import + key）：**
```diff
+ import os
  from .prompt_llm import *
  from .Scenario_description import Scenario
  ...
- api_key = "your key here"
+ # 切到硅基流动（兼容 OpenAI 接口），API Key 通过环境变量传入
+ api_key = os.getenv("SILICONFLOW_API_KEY")
+ if not api_key:
+     raise ValueError("未找到环境变量 SILICONFLOW_API_KEY，请在服务器上 export SILICONFLOW_API_KEY=sk-xxx")
+ SILICONFLOW_BASE_URL = "https://api.siliconflow.cn/v1"
+ SILICONFLOW_MODEL = "Qwen/Qwen2.5-7B-Instruct"
```

**第 140-147 行（`send_to_chatgpt` 函数）：**
```diff
  def send_to_chatgpt(self, ego_veh, current_scenario, negotiation_results, memory):
-     proxy_url = "http://127.0.0.1:7890"
-     import httpx
-     http_client = httpx.Client(proxies={"http://": proxy_url, "https://": proxy_url})
-
-     client = OpenAI(api_key=api_key,  # put your api key here
-                     base_url="https://api.openai.com/v1",
-                     http_client=http_client)
+     # 硅基流动兼容 OpenAI 接口，无需代理
+     client = OpenAI(api_key=api_key,
+                     base_url=SILICONFLOW_BASE_URL)
```

**第 181 行（模型名）：**
```diff
  completion = client.chat.completions.create(
-     model="gpt-4o-mini",
+     model=SILICONFLOW_MODEL,  # 硅基流动的 Qwen2.5-7B-Instruct
      messages=[{"role": "system", "content": prompt},])
```

---

### 2. `llm_controller/llm_agent_negotiation_system.py`

同样的三处改动：
- 头部加 `import os` + 改 `api_key` + 加常量
- `send_to_chatgpt` 函数删 httpx 代理，改 `base_url`
- 模型名 `gpt-4o-mini` → `SILICONFLOW_MODEL`

---

### 3. `llm_controller/memory.py`

**核心改动：用自定义 `SiliconFlowEmbeddings` 替代 `OpenAIEmbeddings`**

```diff
- from langchain.embeddings.openai import OpenAIEmbeddings
+ from langchain.embeddings.base import Embeddings

- api_key = 'your key here'
- os.environ["OPENAI_API_KEY"] = api_key
+ SILICONFLOW_API_KEY = os.getenv("SILICONFLOW_API_KEY")
+ ...
+ SILICONFLOW_EMBEDDING_MODEL = "BAAI/bge-m3"

+ class SiliconFlowEmbeddings(Embeddings):
+     """兼容 langchain Embeddings 接口的硅基流动嵌入类，调用 BAAI/bge-m3。"""
+     def __init__(self, api_key=None, model=SILICONFLOW_EMBEDDING_MODEL,
+                  base_url=SILICONFLOW_BASE_URL):
+         self.client = OpenAI(api_key=api_key or SILICONFLOW_API_KEY, base_url=base_url)
+         self.model = model
+     def embed_documents(self, texts):
+         response = self.client.embeddings.create(model=self.model, input=texts)
+         return [item.embedding for item in response.data]
+     def embed_query(self, text):
+         response = self.client.embeddings.create(model=self.model, input=[text])
+         return response.data[0].embedding

  class DrivingMemory:
      def __init__(self, env) -> None:
-         self.embedding = OpenAIEmbeddings()
+         self.embedding = SiliconFlowEmbeddings()
```

## ⚠️ 影响范围

| 模块 | 是否影响实际运行 | 说明 |
|------|----------------|------|
| `llm_agent_action.py` | ✅ 是 | 主决策模块，**必须**改 |
| `llm_agent_negotiation_system.py` | ✅ 是 | 主协商模块，**必须**改 |
| `memory.py` | ⚠️ 默认未启用 | `llm_agent_action.py` 中 `past_memory = ''`、memory_update 被注释，**当前不会调用**。改是为后续启用做准备 |

## 📌 服务器端配套操作

切换后，**服务器必须**设置环境变量，否则启动会 `ValueError` 报错：

```bash
# 在 conda 环境中（推荐写入 ~/.bashrc 或 conda activate 后设置）
export SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 临时验证一下
echo $SILICONFLOW_API_KEY
```

## 🔑 硅基流动 Key 获取

1. 打开 https://cloud.siliconflow.cn/account/ak
2. 登录后创建 API Key
3. 复制 key（`sk-` 开头）

## 🧪 冒烟测试建议

服务器上做完 `export` 后，先改 `Run_multi_CAV_LLM.py:58` 的 `range(100)` 为 `range(2)`，跑一次看是否能调用成功：

```bash
cd CoDrivingLLM
python Run_multi_CAV_LLM.py
```

观察要点：
- 是否成功打印 `llm_actions` 和 `global_reward`
- `llm_controller/video/0.mp4` 和 `1.mp4` 是否生成
- `llm_controller/excel/0.xlsx` 是否生成
- 是否有 `openai` 报错

## 🔄 切换其他模型（如果需要）

只需改 3 个常量即可（**两处都要改**，因为协商和决策用了相同常量）：

```python
SILICONFLOW_MODEL = "Qwen/Qwen2.5-7B-Instruct"           # 7B 免费
# SILICONFLOW_MODEL = "Qwen/Qwen2.5-14B-Instruct"        # 14B
# SILICONFLOW_MODEL = "Qwen/Qwen2.5-32B-Instruct"        # 32B
# SILICONFLOW_MODEL = "Qwen/Qwen2.5-72B-Instruct"        # 72B
# SILICONFLOW_MODEL = "deepseek-ai/DeepSeek-V2.5"        # DeepSeek
```

嵌入模型同理改 `SILICONFLOW_EMBEDDING_MODEL`：
- `BAAI/bge-m3`（默认，推荐）
- `BAAI/bge-large-zh-v1.5`
- `Pro/BAAI/bge-m3`（加速版）
